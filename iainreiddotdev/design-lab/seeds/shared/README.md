# Design lab shared notes

Prototype pages keep direction CSS/JS local so each alternative stays independently reviewable.

Approved images are referenced with relative paths into `docs/assets/images/` and are not duplicated here.

These prototypes are isolated experiments. They must not be linked from production navigation or imported into production templates.
